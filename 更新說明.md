# 軋軋 V4.13 整合更新

本版以你提供的 V4.12 為底稿，保留每日低風險清單，整合既有個股風險河流圖與 AI Overheat Monitor。

## 更新內容
- 個股首頁：保留 MA13 / MA40 / MA63 / MA150 / MA1000 與風險河流圖，低風險區改為綠色、中風險區改為黃色。
- 河流圖沿用原版計算；彩色帶表示本次分析的價格區間，不是歷史每日重新計算的風險帶。
- 每日清單：保留 V4.12 的市場、掃描規模、產業、完整確認檔數及新進／持續／首次確認篩選。
- 新增 AI Overheat Monitor 頁面，保留七大雷達、硬紅旗、新聞、台灣供應鏈及歷史功能。
- 三頁主畫面都有切換入口，手機不必只靠側邊欄尋找。
- AI 模組獨立存放於 ai_monitor；歷史資料使用 data/ai_overheat/history.csv，與每日清單分開。

## 更新方式
1. 先備份目前專案，尤其 data 資料夾及 .streamlit/secrets.toml（若有）。
2. 將此 ZIP 解壓縮，把裡面的檔案與資料夾合併覆蓋到原專案根目錄（app.py 所在位置）。請保留現有 data 和個人設定。
3. 若舊專案還留有 pages/1_個股分析.py，刪除該舊頁；V4.13 個股首頁是 app.py，避免多出舊版入口。
4. 執行 `python -m pip install -r requirements.txt`。
5. 執行 `python -m streamlit run app.py`。Windows 亦可雙擊 start_windows.bat。
6. 若在 Streamlit Community Cloud 更新，將新檔案及 requirements.txt 一起更新到原儲存庫，入口維持 app.py，然後重新啟動 App。

AI Monitor 開啟頁面時會抓新聞與行情，第一次可能需要等候。資料来源沿用原專案的 Google News RSS、Yahoo Finance；個股營收和股票池沿用 FinMind。

選用：執行 `python scripts/ai_daily_snapshot.py` 儲存 AI 歷史快照；本版不會自動替你設定排程。

更新包不含 Token、個人歷史資料或虛擬環境。原版文件位於 README.md，AI 專案文件位於 ai_monitor/README.md（其中原獨立專案路徑請以本文為準）。

## 驗證紀錄
- Python 語法檢查通過。
- Streamlit AppTest：個股分析與河流圖、每日清單篩選、頁面切換、AI 五個子分頁與重新掃描通過。
- 新聞為空及部分請求失敗情境通過；修正原 AI 模組在上述情境的例外。
- 測試採固定行情及新聞資料，不代表已驗證外部來源的即時可用性。

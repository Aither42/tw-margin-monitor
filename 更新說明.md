# V4.14 更新方式

這份是完整整合包：軋軋個股分析＋每日低風險清單＋AI Overheat Monitor＋台股每日融資風險河流圖。

1. 先備份原專案，尤其 data 與 .streamlit/secrets.toml（若有）。
2. 解壓後，把檔案及資料夾合併覆蓋到原專案的根目錄，也就是 app.py 所在資料夾。你電腦上原 stock 專案位於 `C:\Users\hp\Documents\GitHub\stock`。
3. 保留現有 data 與個人設定。本包不包含個人資料、Token 或已抓取的歷史資料庫。
   建議在你原本 .gitignore 加上 `data/` 與 `.streamlit/secrets.toml`，避免把個人資料與 Token 提交到 GitHub。
4. 如果 pages 裡還有舊 `1_個股分析.py`，移除該舊頁以免重複；個股首頁仍使用根目錄 app.py。
5. 執行 `python -m pip install -r requirements.txt` 更新依賴。這次新增 lxml，供上櫃 HTML 備援解析。
6. 執行 `python -m streamlit run app.py`，或雙擊 start_windows.bat。
7. 若透過 GitHub＋Streamlit 更新，將所有新檔案和 requirements.txt 一起更新，入口仍是 app.py，再重新啟動 App。

融資圖在首頁下方，也有獨立入口。第一次按「載入／更新並補抓缺日」建立歷史；後續會保留成功資料，再補抓缺日。

完整更動及資料限制請看 README_V4.14.md。舊 README.md 留作先前版本說明，以 README_V4.14.md 為準。
